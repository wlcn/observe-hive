package org.now.wlcn.observe.hive.api;

public enum MetricType {
    MEMORY_HEAP,
    MEMORY_NON_HEAP,
    THREAD_COUNT,
    THREAD_STATE,
    GC_COUNT,
    GC_TIME,
    CLASS_LOADING,
    SYSTEM_LOAD,
    CPU_USAGE
}
